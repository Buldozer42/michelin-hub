api_platform:
    title: Hello API Platform
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
    swagger:
        persist_authorization: true
        http_auth:
            JWT:
                scheme: bearer
                bearerFormat: JWT